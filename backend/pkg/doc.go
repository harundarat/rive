/*
Package pkg contains generic, domain-agnostic utilities, libraries, and helpers.

Code within this package must not have any dependencies on the application's
internal business logic. Examples include custom loggers, hashing utilities,
or generic data type conversion libraries that can be reused across multiple projects.
*/
package pkg
