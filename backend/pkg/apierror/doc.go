/*
Package apierror defines typed API errors with HTTP status codes and
machine-readable error codes for consistent error responses.

Each error type carries an HTTP status code, a human-readable message,
and an application-level error code so clients can handler errors
programmatically without parsing message strings.
*/
package apierror
