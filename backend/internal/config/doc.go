/*
Package config provides data structures and functions to load application configurations.

It reads environment variables or configuration files and converts them
into strongly-typed structs. This ensures that the configurations can be
used safely and reliably by other components within the application.
*/
package config
