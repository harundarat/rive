/*
Package domain contains the core entity definitions and interfaces (contracts).

This is the innermost layer of the Clean Architecture. The domain defines
"what" the system does through Repository and Usecase interfaces, without
caring about "how" it is implemented. This package must not depend on
any other layers or external infrastructure libraries.
*/
package domain
