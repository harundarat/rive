/*
Package usecase contains the core business rules and logic of the application.

This layer acts as an orchestrator that receives input from the presentation layer,
executes business rules, and interacts with the data infrastructure strictly
through domain interfaces. This layer is completely independent of UI, HTTP frameworks,
or specific database implementations.
*/
package usecase
