/*
Package response defines structs that represent outbound HTTP response payloads.

Each struct is serialized to JSON and returned to the client. Response types
wrap domain data into a stable, versioned contract that shields clients from
internal domain model changes.
*/
package response
